# UI/UX Polish

## Closed Alphaで実施済み

- pass / warning / fail を同じ語彙で統一。
- 手動確認が必要な warning を明示。
- ホストUIは最小表示に抑え、検証は CLI と代表データで再現可能にした。

## MVP後の磨き込み

- 実データ投入時の入力補助。
- warning の理由別フィルタ。
- release evidence へのワンクリック書き出し。
- popup と side panel の分離。

